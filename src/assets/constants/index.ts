import { ConstantsTypes } from "../../types";

export const constants: ConstantsTypes = {
    endpoints: {
        search: "/search",
    },
    paths: {
        search: "/",
        show: "/show",
    },
};
